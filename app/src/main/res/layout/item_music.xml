<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardCornerRadius="8dp"
    app:cardElevation="4dp"
    android:layout_marginBottom="8dp"
    android:layout_marginEnd="8dp"
    android:layout_marginStart="8dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="8dp">

        <!-- ImageView to show album art -->
        <ImageView
            android:id="@+id/music_image_view"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:layout_marginEnd="16dp"
            android:scaleType="fitCenter"
            android:src="@drawable/music" /> <!-- Make sure 'music' drawable is present -->

        <!-- TextView to show music title -->
        <TextView
            android:id="@+id/music_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Music Title"
            android:textSize="16sp"
            android:layout_weight="1"
            android:ellipsize="end"
            android:maxLines="1" />

        <!-- ImageView for Play/Stop -->
        <ImageView
            android:id="@+id/play_stop_image_view"
            android:layout_width="45dp"
            android:layout_height="59dp"
            android:layout_marginStart="16dp"
            android:contentDescription="Play/Stop Button"
            android:src="@drawable/play1" />
    </LinearLayout>
</androidx.cardview.widget.CardView>
